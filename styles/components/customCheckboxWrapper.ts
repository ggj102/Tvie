import styled from "styled-components";

export const CustomCheckboxWrapper = styled.div`
  label {
    margin: 0;
    display: flex;
    gap: 4px;
    align-items: center;
  }

  label > span {
    padding: 0;
  }

  label > p {
    line-height: 16px;
  }
`;
