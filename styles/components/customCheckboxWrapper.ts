import styled from "styled-components";

export const CustomCheckboxWrapper = styled.div`
  display: flex;
  align-items: center;

  label {
    margin: 0;
    gap: 4px;
  }

  label > span {
    padding: 0;
  }

  label > p {
    line-height: 16px;
  }
`;
